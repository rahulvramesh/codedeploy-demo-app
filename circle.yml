machine:
  php:
    version: 5.6.5

dependencies:
  cache_directories:
    - vendor
    - node_modules
  pre:
    - sudo pip install awscli

test:
  production:
    branch: master
    commands:
      - ./.circle/deploy-production.sh
